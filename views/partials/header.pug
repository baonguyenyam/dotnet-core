header.navbar.navbar-dark.bg-dark.navbar-expand-md.fixed-top
	.d-flex.w-100
		a.navbar-brand(href='/admin/')
			i.fas.fa-cube 
			| 
			| ngCMS
		button.navbar-toggler(type='button', data-toggle='collapse', data-target='.navbar-collapse')
			span.sr-only Toggle navigation
			span.navbar-toggler-icon
		.collapse.navbar-collapse
			ul.nav.navbar-nav
				li.nav-item(class=(title === 'Home') ? 'active' : undefined)
					a.nav-link(href='/admin/') Home
				li.nav-item(class=(title === 'API Examples') ? 'active' : undefined)
					a.nav-link(href='/admin/api') API Examples
				li.nav-item(class=(title === 'Contact') ? 'active' : undefined)
					a.nav-link(href='/admin/contact') Contact
				li.nav-item(class=(title === 'Status') ? 'active' : undefined)
					a.nav-link(href='/status') Status
			ul.nav.navbar-nav.ml-auto
				if !user
					li.nav-item(class=(title === 'Login') ? 'active' : undefined)
						a.nav-link(href='/admin/login') Login
					li.nav-item(class=(title === 'Create Account') ? 'active' : undefined)
						a.nav-link(href='/admin/signup') Create Account
				else
					li.dropdown(class=(title === 'Account Management') ? 'active' : undefined).nav-item
						a.dropdown-toggle.nav-link(href='#', data-toggle='dropdown')
							if user.profile.picture
								img(src=user.profile.picture)
							else
								img(src=user.gravatar(60))
							span= user.profile.name || user.email || user.id
							i.caret
						ul.dropdown-menu
							li.dropdown-item
								a(href='/admin/account') My Account
							li.divider.dropdown-item
							li.dropdown-item
								a(href='/admin/logout') Logout
.nav-scroller.bg-white.box-shadow
	nav.nav.nav-underline
		a.nav-link.active(href="#") Dashboard
		a.nav-link(href="#")
			| Friends
			span.badge.badge-pill.bg-light.align-text-bottom 27
		a.nav-link(href="#") Explore
		a.nav-link(href="#") Suggestions
		a.nav-link(href="#") Link
		a.nav-link(href="#") Link
		a.nav-link(href="#") Link
		a.nav-link(href="#") Link
		a.nav-link(href="#") Link
